import React from 'react';
import ReactDOM from 'react-dom/client';
import ProDirecciones from './proDirecciones';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
      <ProDirecciones />
  </React.StrictMode>
);


